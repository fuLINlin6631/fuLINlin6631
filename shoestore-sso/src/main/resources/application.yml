server:
  port: 10001
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/shoe?characterEncoding=utf-8&useSSL=false
    username: root
    password: root
    driver-class-name: com.mysql.jdbc.Driver
    type: com.alibaba.druid.pool.DruidDataSource
  redis:
    port: 6379
    host: localhost
    database: 0
mybatis:
  mapper-locations: classpath*:mapper/*.xml